# Assets Folder

Coloque aqui a imagem do logotipo STAMPLY.

**Nome do arquivo:** `stamply-logo.png` (ou `.svg`, `.jpg`, `.jpeg`)

O código está configurado para importar automaticamente a imagem com o nome `stamply-logo.png`.

Se usar um nome diferente ou formato diferente, atualize o import no arquivo `src/components/Navbar.tsx` na linha 7.

